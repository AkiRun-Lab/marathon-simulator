# 更新履歴 (CHANGELOG)

このファイルはマラソンペース計算ツール（MPC）の更新履歴を記録します。

---

## [1.6.1] - 2026-07-15

起伏コース（獲得標高300m以上）判定時のCTA送客先 `AMAZON_STRENGTH_LIST_URL` を、ストアトップ暫定（リスト⑦）から発行済みの臀筋・体幹アイデアリスト（`https://amzn.to/4fxnEAV`・トラッキングID akirun-rfd-22）に差し替え。起伏コースの脚づくり（殿筋・体幹）へ直接送客する。

---

## [1.6.0] - 2026-07-14

シミュレーション結果に応じてCTAをAmazonカテゴリ別リストへ動的に振り分ける機能を追加（AMCの計画連動シューズCTAのパターンを移植）。

### 追加
- `lib/cta_selector.py`：気温補正遅延・風速・獲得標高・目標タイムから送客カテゴリを判定する純粋関数 `judge_cta_category()`。優先順位は暑さ大（+5分以上）→暑さ中（+2分以上）→強風（3m/s以上）→起伏大（獲得標高300m以上）→目標タイム（4時間以上でデイリートレーナー、未満でレースシューズ）。例外を投げず、不正入力は安全側のデフォルトに倒す。
- CTA①（結果カード内）：上記カテゴリに応じて、暑さ→補給・サプリ、強風→ゼッケン・小物、起伏→筋トレ・補強、それ以外→レースシューズ/デイリートレーナーのAmazonリストへボタンとURLを動的に切り替え。該当カテゴリでは想定気温・遅延・風速・獲得標高を伝える一言キャプションを追加。組み立てに失敗した場合は従来どおりの目標タイム帯別ラベル＋ストアトップURLにフォールバックし、アプリを壊さない。
- カテゴリ別Amazonリスト定数を追加（`AMAZON_RACE_SHOES_LIST_URL`・`AMAZON_DAILY_TRAINER_LIST_URL`・`AMAZON_STRENGTH_LIST_URL` は発行済み短縮URL。ウェア・ガジェット・補給・ゼッケン小物の4つは短縮URL未発行のためストアトップにフォールバック）。
- `tests/test_cta_selector.py` を追加（境界値・優先順位・不正入力のテスト27件）。

### 変更
- CTA②（ラップ表下）：見出し・サブ文・ボタンをランニングギア・ガジェット訴求に更新し、送客先をAmazonカテゴリ別リスト定数（`AMAZON_GADGET_LIST_URL`）に変更（分岐なしの固定CTA）。
- CTA③（コース比較後）は変更なし。

---

## [1.5.1] - 2026-07-03

コードレビュー（ルートリポジトリ `docs/code-review/mss-2026-07-03.md`）に基づく修正。

### 修正（フェーズ1：重大）
- **コース比較の標高補正を修正（R-1）**：比較先コースのシミュレーションに現在コースの標高遅延が持ち込まれ、比較先自身の平均標高が無視されていた問題を修正。比較時は気温補正のみのタイムを基準に、比較先コースの平均標高で標高補正を再計算する。高地コース（富士山・榛名湖・ノースオンタケ・大町アルプス等）が絡む比較タイム・おすすめ判定が正しくなった。
- コース比較の表示に「平均標高」を追加（両コース・差分付き）。
- 補正モジュールのimportをファイル先頭に整理（関数内importの重複を解消）。

### 修正（フェーズ2：堅牢化）
- **目標タイム入力のバリデーション追加（Y-1）**：「0:00:00」入力で例外画面（ゼロ除算）になる問題を修正。分秒の範囲（0〜59）と現実的なタイム範囲（1:00:00〜7:00:00）を検証し、範囲外はエラー表示。`PacingStrategy` の 0 判定も是正。
- **走行不能セグメントの9999秒加算を廃止（Y-2）**：ソルバーが解を返せない急勾配で5m区間に9999秒（約2.8時間）が加算され合計タイムが崩壊する問題を修正。下限速度で実距離ぶんの時間を積算する方式に変更。パワー乗数に下限クランプ（0.3）を追加し、坂道強度70%×急勾配で負のパワーになる経路を遮断。
- **標高タグのないGPXを明示エラーに（Y-3）**：`<ele>` が全欠損のGPXはTypeErrorクラッシュではなく「標高データが含まれていません」のエラー表示に。一部欠損は線形補間で継続。
- **静かなフォールバックを廃止（Y-4）**：GPXファイルが読めない場合に通知なしで愛媛簡易モデルの結果を表示していた挙動を廃止し、明示的なエラー表示に変更（本体・コース比較とも）。VDOTデータ欠落時の機能しない代替入力欄も削除。
- 堅牢化のテスト5件を追加（`tests/test_robustness.py`）。

### 整理（フェーズ3：クリーンアップ）
- シミュレーション総距離の5m過大計上を修正（G-7）：終点42.195km地点にも5m区間を加算していたため合計が42.200km分になっていた。予想タイムが約1〜2秒短くなる。
- 非推奨APIを更新（G-8）：`Scattermapbox`→`Scattermap`、`st.dataframe` の `use_container_width`→`width="stretch"`（`st.plotly_chart` はstreamlit 1.50時点で `width` 未対応のため据え置き）。requirementsの下限を streamlit>=1.49 / plotly>=5.24 に引き上げ。
- `pytest` を `requirements-dev.txt` に分離（G-3）：本番Streamlit Cloudへの不要インストールを解消。
- デバッグ・検証スクリプト4本を `scripts/` へ移動（G-6）。実行方法は `scripts/README.md` 参照。
- 未使用コードを削除（G-4）：`calculate_difficulty_score`・`get_ehime_marathon_default`・`get_segment_at_km`（course_data）、`get_adjusted_pace_per_km`（temperature_adjustment）、`get_vdot_options`・`get_seconds_for_vdot`（vdot_handler）、`PacingStrategy` の未使用 `vdot` 引数と `self.mass` 二重代入。
- 過去のAI編集セッションの残骸コメント（app.py 12行）と重複したsession_state初期化を削除（G-1・G-2）。
- bare `except:` を具体的な例外型に変更（G-5・vdot_handler）。
- docstringの例示値を実計算に一致させ（G-9・気温補正 189.2→189.4 等）、「コース難易度」のキャプションに気象・標高の影響が含まれる旨を明記。
- サンプリング距離のテスト1件を追加（計47件パス）。

---

## [1.5.0] - 2026-06-03

### 変更
- ギア訴求CTA（結果カード内ボタン・ラップ表下・コース比較後メインCTA）の送客先を、ブログのギアガイドからAmazonおすすめギア一覧（ストアフロント）へ変更。購買導線を最短化。
- コース比較後のメインCTAを、Amazonへ直送するゴールド系の大型カードに刷新（「🔥ランナーに人気」バッジ・大ボタン）。RFD/SDTの `render_gear_cta()` デザインを踏襲。
- 全Amazonリンクに `rel="noopener noreferrer sponsored"` を付与し、適格性開示文を表示。

### 維持
- フッターのギアガイドリンクはブログ送客（PV/AdSense）の副導線として1枚残置。使い方ガイド等のナビリンクは据え置き。

---

## [1.4.2] - 2026-04-22

### 追加
- **コース追加（国内）**: 高地コース4コースを新規追加
  - ノースオンタケマラソン
  - 榛名湖マラソン
  - 富士山マラソン
  - 大町アルプスマラソン

---

## [1.4.1] - 2026-04-19

### 追加
- **標高補正閾値を開発者用 UI で変更可能に**: `?dev=true` でアクセスした際のフォームに「詳細設定: 標高補正の閾値 (m)」スライダー（0〜1500m、50m刻み、デフォルト 500m）を追加
  - コード変更・再デプロイなしに閾値を試行錯誤できる
  - 通常ユーザーへの表示・挙動は変化なし

### 変更
- `altitude_adjustment.adjust_marathon_time()` / `get_delay_minutes()` に省略可能な `threshold_m` 引数を追加（省略時はモジュール定数 `ALTITUDE_THRESHOLD_M` を使用）
- 結果エリアの「影響なし」info メッセージの閾値表示を固定値 500 から実際の適用値（`result_meta['altitude_threshold_m']`）に動的化

---

## [1.4.0] - 2026-04-20

### 追加
- **標高補正機能**: GPX の絶対標高（海抜 m）からコースの平均標高を算出し、Péronnet et al. (1991) モデルに基づくタイム補正を実装
  - サイドバーに「🏔️ 標高補正を適用」チェックボックスを追加（デフォルト ON）
  - 気温補正と同様の乗算補正式。500m 以下の低地コースは補正ゼロ
  - 結果エリアに「平均標高 Xm → +Y.Y分」の info メッセージと「平均標高」metric を追加
  - 1500m 超・2500m 超で段階的な注意メッセージを表示
  - 開発者キルスイッチ `ALTITUDE_CORRECTION_ENABLED` と閾値 `ALTITUDE_THRESHOLD_M` を `altitude_adjustment.py` 冒頭に配置

### 変更
- `CourseSegment` dataclass に `start_elevation: Optional[float]` フィールドを追加（非破壊）
- `CourseData` クラスに `calculate_mean_elevation()` メソッドを追加
- `GPXHandler.to_course_data()` で GPX の `ele_smooth` を各セグメントの `start_elevation` に伝搬

### 新規ファイル
- `lib/altitude_adjustment.py`: Péronnet (1991) 補正ロジック
- `tests/test_altitude_adjustment.py`: 16 ケースのユニットテスト

---

## [1.3.2] - 2026-03-24

### 変更
- **コースファイルの整理**: GPXファイルを `data/domestic/`（国内）・`data/international/`（海外）のサブフォルダに分類
  - ドロップダウンメニューが国内コース→海外コースの順で表示されるよう改善
  - 海外コースのGPXファイル名を日本語に統一（Berlin_marathon.gpx → ベルリンマラソン.gpx 等）
  - 新規コース追加時は対応するフォルダにGPXファイルを置くだけでOK（コード変更不要）

---

## [1.3.1] - 2026-03-19

### 変更
- **アプリ表示名の変更**: 「マラソン攻略シミュレーター」→「マラソンペース計算ツール（MPC）」に統一
  - `st.set_page_config`、UIタイトル、フッター表示に反映
  - README.md・USER_MANUAL.md・SPECIFICATION.md のタイトルも更新

---

## [1.3.0] - 2026-03-08

### 追加
- **風向き16方位対応**: 風向き選択肢を8方位（45度刻み）から16方位（22.5度刻み）に拡張
  - 北北東、東北東、東南東、南南東、南南西、西南西、西北西、北北西の8方位を追加

---

## [1.2.1] - 2026-02-22

### 修正
- **起動時エラー修正**: リブート直後にKeyErrorが発生する不具合を修正（セッションステート初期化漏れ）

### ドキュメント
- USER_MANUAL.md、SPECIFICATION.md、README.md、DEPLOYMENT.mdを最新版に更新

---

## [1.2.0] - 2026-02-07

### 変更
- **コース難易度の定義変更**: 「シミュレーション結果 / 目標タイム」に変更（例: 1.027 = 約2.7%増）
- **設定パネルのアコーディオン廃止**: 常時展開表示に変更
- **アフィリエイトCTA追加**: コース比較セクション後にギア紹介リンクを配置

### 削除
- 旧難易度指標（Course Toughness Index）を廃止

---

## [1.1.0] - 2026-01-28

### 追加
- **気温補正機能**: レース当日の気温(0〜35°C)に基づいて予想タイムを自動調整
  - Ely et al. (2007), El Helou et al. (2012) の学術研究に基づく計算式を実装
  - 最適気温10°C、それ以上で気温に応じた遅延時間を計算
  - 気温補正情報を結果画面に表示（補正適用時のみ）
  - 25°C以上で高温注意警告、28°C以上で危険警告、30°C以上で極めて危険警告を表示
  - 免責文言を表示（個人差・コンディション差への配慮）

### 技術詳細
- 新規モジュール `lib/temperature_adjustment.py` を追加
- 気温スライダーをUIに統合（風速・風向きと並列配置）
- シミュレーション計算ロジックに気温補正を統合
- 単体テスト17件を実装、全テスト通過
- ブラウザ統合テストで気温20°C/10°Cでの動作を検証

---

## [1.0] - 2026-01-26

### 追加
- **獲得標高表示**: メインシミュレーション結果に獲得標高を表示
- **コース難易度表示**: メインシミュレーション結果にコース難易度を表示

### 変更
- メトリクス表示を2行構成に変更（1行目：タイム・ペース・基礎走力、2行目：獲得標高・難易度）
- バージョン番号をβ0.1から1.0に変更（正式版リリース）

---

## [β0.1.1] - 2026-01-17

### 追加
- **開発者プロフィール**: フッターに開発者情報（あきら、PB 2:46:27）を追加
- **ブログリンク**: AkiRunブログおよび使い方ガイドへのリンクをフッターに追加
- **予想タイム強調表示**: シミュレーション結果の予想タイムを大きなカード形式で目立つように表示
- **コース比較おすすめコメント**: コース比較結果に「PR狙いにおすすめ」等の分かりやすいコメントを追加

### 変更
- フッターにバージョン情報とコピーライトを表示

---

## [β0.1] - 2026-01-07

### 初期リリース
- GPXデータによるコース解析
- Minetti et al. (2002) の物理モデルに基づくペース計算
- VDOT / 目標タイムからの走力設定
- 風速・風向きの影響を考慮
- スプリット配分（イーブン/ポジティブ/ネガティブ）
- 坂道強度設定
- ペース戦略チャート
- コース平面図表示
- 1km毎の区間ラップ表
- コース比較機能
